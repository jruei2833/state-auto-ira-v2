form5500-raw-data/

# Stray notes from unrelated work (federal regulation analysis) that
# accidentally landed in methodology/. Ignored to keep `git status` clean.
methodology/04 exp.txt
methodology/MAC 2625-1-1.17 Is not defined as b.txt

# Python
__pycache__/
*.pyc
*.pyo
*.log

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Build artifacts (markdown is source, docx is derived).
# Keep stakeholder hand-off docx tracked so they're findable on GitHub
# without requiring `make` first.
deliverables/*.docx
deliverables/archive/*.docx
!deliverables/project_results_memo.docx
!deliverables/count_reconciliation_memo.docx

# R
.Rhistory
.RData
.Ruserdata

# Bulk data downloads (large, regenerable)
data/bls_qcew/raw/
data/census_susb/raw/
data/dol_form5500/raw/
*.zip

# Pre-v1/v2-split single-version build artifact (Feb 2026); regenerable
data/processed/
